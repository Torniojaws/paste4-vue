All non-JS files that are used in the views, like background images, inline images, etc

For non-display items and files not used by webpack, eg. file downloads, use the usual static/ directory structure.
